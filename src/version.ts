/**
 * Semver, bumped by hand on each shipped change:
 * MAJOR — breaking change to stored data (would require a "Clear data")
 * MINOR — new feature
 * PATCH — bug fix / polish with no new capability
 */
export const APP_VERSION = "0.8.0";
